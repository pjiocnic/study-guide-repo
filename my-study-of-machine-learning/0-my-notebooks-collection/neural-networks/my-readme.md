Here is your Colab-ready notebook for **multiclass text classification**:

📒 [Download `multiclass_text_classification_nn.ipynb`](sandbox:/mnt/data/multiclass_text_classification_nn.ipynb)

### ✅ Features:

* Uses the real-world **20 Newsgroups** dataset
* Vectorizes raw text using **TF-IDF**
* Implements multiclass classification using:

  * TensorFlow/Keras with `softmax` and `categorical_crossentropy`
  * PyTorch with `CrossEntropyLoss`
* Evaluates accuracy for both models

Let me know if you’d like to extend it with:

* Pretrained embeddings (e.g., GloVe, BERT)
* Confusion matrix or F1-score
* Longer training or early stopping
