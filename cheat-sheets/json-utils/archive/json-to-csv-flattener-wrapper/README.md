# JSON to CSV Flattener with Wrappers

Reads multiple JSON files from the `jsons/` directory and flattens them into a single CSV using wrapper classes for extensibility.

## Usage
```bash
chmod +x run.sh
./run.sh
```
