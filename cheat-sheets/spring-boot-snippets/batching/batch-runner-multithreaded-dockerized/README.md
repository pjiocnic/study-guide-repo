# Batch Runner Multi-threaded (Dockerized)

This is the Dockerized version of the multi-threaded batch runner.

## Build

```bash
docker build -t batch-runner-mt .
```

## Run

```bash
docker run --rm batch-runner-mt
```
